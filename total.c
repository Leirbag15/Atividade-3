#include <stdio.h>
// essa função vai ficar redundante porque C fundamentalmente trabalha com "listas" (array) já sabendo o tamanho máximo delas

int total (int v[]){
    int t=0
    for (int i=0; i<n; i++){
        t++;
    }
    return t;
}
int n; //size 
int main()
{
    int v[n];
    for (int i=0; i<n; i++){
        scanf("%d", &v[i]);
    }
}
